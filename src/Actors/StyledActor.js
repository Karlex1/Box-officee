import styled from "styled-components";
import { Searchcard } from "../components/styled";

export const ActorStyled = styled(Searchcard)`
 .deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`